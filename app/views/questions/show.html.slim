= link_to 'Delete the Question', question_path(@question), method: :delete if current_user&.author_of?(@question)
h1 = @question.title
p = @question.body


.answers
  = render @question.answers

.answer-errors

= render 'answers/form'
